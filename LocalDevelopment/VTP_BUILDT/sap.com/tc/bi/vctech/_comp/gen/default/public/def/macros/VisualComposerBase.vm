#**
 * Default invocation of the Visual Composer model generator. The only parameters
 * you need to define are:
 *
 * sources      list of source paths for the Visual Composer generator.
 *
 * targetwd     destination folder for generated WebDynpro sources
 *
 * targetportal destination folder for generated Portal Content data
 *#
#macro(DefaultVCModelGeneration $sources $targetwd $targetportal)
    $visualComposerBase.defaultVCModelGeneration($sources, $targetwd, $targetportal)
#end

#macro(DefaultVCModelGeneration2 $sources $outputmap)
    $visualComposerBase.defaultVCModelGeneration($sources, $outputmap)
#end
